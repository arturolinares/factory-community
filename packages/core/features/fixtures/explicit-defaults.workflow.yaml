# Every value here is also the default. They are written out on purpose, so a
# reader does not have to know the defaults to understand the file.
name: release
mode: once
scheduling: parallel
phases: []
