---
name: Something does not work
about: A behaviour that contradicts what the documentation says
labels: bug
---

**What happened, and what you expected instead.**

**Where the documentation says otherwise** — a file and a line, if you can. If Factory promised
something it did not do, that is the most useful kind of report this project gets.

**To reproduce**

```
```

**Your installation**

- Factory: commit or version
- `node --version`, `pnpm --version`, OS
- Execution profile: Default or Full Access
- Output of `factory doctor` (it is short, and it usually knows)

**A failing scenario, if you managed one.** A `.feature` snippet that fails is worth more than any
description, because it becomes the regression test.
